# yaml-language-server: $schema=https://raw.githubusercontent.com/roshbhatia/gate/main/schema/provider.schema.json
version: provider/v1
name: notes
description: Put the owner's open review notes in front of the model; `note` writes and reads the record
command: [gate-provider-notes]
actions:
  gate.decide:
    description: UserPromptSubmit
    argv: [serve]
defaults:
  timeout: 2s
